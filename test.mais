typedef static Player {
	x: dd
	y: dd

	Player() {
		this.x = 3
		this.y = 3
	}
	
	draw() {
		Console.move(this.x, this.y)
		Console.attron(A_BLINK)
		Console.addch('*')
		Console.attroff(A_BLINK)
	}
}

init() {
	
}

draw() {
	Player.draw()
}

key(key: dd ?) {

}

mouse(x: dd ?, y: dd ?) {

}